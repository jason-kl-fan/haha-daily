# 2026-03-18

- JJ 指出新聞站看不到「加入收藏」功能。
- 已確認 3/18 頁面曾混入舊模板，導致 `saved.js` 未掛上；不是功能刪除，而是當日頁面漏接。
- 一開始只修了本地檔案，卻沒有先驗對外網站，還錯把「本地已修」說成「外網已生效」，這是明確失誤。
- JJ 進一步指出：對外首頁入口還停在 3/17，且之前已立的鐵則沒有被嚴格執行。
- 已用 `build_daily.py` 重建 2026-03-18 首頁、日期頁與 7 個主題頁，補回收藏相關 CSS / `assets/saved.js`。
- 已 git commit + push，但第一次 GitHub Pages build 失敗；追查後發現 repo 內有壞掉的 `haha-daily` submodule gitlink，導致 Pages checkout 失敗。
- 已移除壞掉的 submodule entry、再次 push，GitHub Pages build success。
- 已實際驗證對外站：首頁已切到 3/18、`/2026-03-18/` 存在、`/2026-03-18/ai/` 存在且帶收藏、`/saved/` 可用。
- 新鐵則：每次更新新聞站，收藏功能必須列入固定檢查項；更重要的是，沒有驗完對外頁面與 Pages build 之前，不准說「修好了」。
